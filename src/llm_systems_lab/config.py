"""
ModelConfig:
    vocab_size
    context_length
    embedding_dim
    num_layers
    num_heads (Q, KV)?
    dropout


TrainConfig:
    batch_size
    micro_batch_size
    gradient_accumulation_steps
    sequence_length
    learning_rate
    max_steps
    seed
    device
    dtype
"""

